services:
  mcp-path-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-path-service
    restart: unless-stopped
    ports:
      - "8001:8000"
    environment:
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - NEBULA_HOST=${NEBULA_HOST:-host.docker.internal}
      - NEBULA_PORT=${NEBULA_PORT:-9670}
      - NEBULA_USER=${NEBULA_USER:-root}
      - NEBULA_PASSWORD=${NEBULA_PASSWORD:-nebula}
      - SEARCH_DEPTH_DAYS=${SEARCH_DEPTH_DAYS:-90}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/openapi.json"]
      interval: 30s
      timeout: 3s
      start_period: 10s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 128M
    volumes:
      - ./logs:/app/logs
    networks:
      - mcp-network

networks:
  mcp-network:
    driver: bridge
